/** @type {import('next').NextConfig} */
// https://console.cloudinary.com/
const nextConfig = {
    images: {
        domains: [
            "res.cloudinary.com"
        ]
    }
};

export default nextConfig;


